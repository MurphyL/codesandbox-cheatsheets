# docker run

```sh
docker run [options] IMAGE
```

# docker exec

```sh
docker exec [options] CONTAINER COMMAND
  -d, --detach        # run in background
  -i, --interactive   # stdin
  -t, --tty           # interactive
```
